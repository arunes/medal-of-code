defmodule MocWeb.StatsController do
  use MocWeb, :controller

  def index(conn, _params) do
    # total prs
    # first pr date
    # last pr date
    # avg pr completion
    # pr per day
    # total open pr time
    # total comments
    # total votes

    # git activity chart

    # counters
    # main counters

    # active repo list

    render(conn, :index)
  end
end
